// if
// if(condition == true ){}
const isuserloggedin = true
if(isuserloggedin){
    console.log("welcome to the website")
}
 // == :- comparison
 // === :- checks the datatype as well
 // = :- assignment operator, it only assigns the value

 if(2==="2"){
    console.log("its equal")
 }
 else console.log("its not equal")

const balance = 1000

// && :- checks both conditions 
// || :- checks either one is true